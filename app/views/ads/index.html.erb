<div class="container">
  <div class="row" id="news">

    <div class="col-xs-12">
      <h1>
        LATEST ADS
      </h1>
      <h2 class="text-center">
        All the recruitment / fundings ad from the community
      </h2>
      <ul id="ad_type_menu">
        <li><%= link_to "All", "/ads" %></li>
        <li><%= link_to "Recruitments", "/ads/recruitment" %></li>
        <li><%= link_to "Fundings", "/ads/funding" %></li>
      </ul>
    </div>
    <% @ads.each do |ad| %>
      <%= render partial: "ads/ad_row", locals: { ad: ad } %>
    <% end %>

  </div>
</div>


<% content_for :meta_title, "Community Ads" %>
<% content_for :meta_description, "All the recruitment / fundings ad from the community" %>
